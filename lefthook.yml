# Configuration Lefthook optimisée pour GymArt
# Automatise la qualité de code et les vérifications avant commit/push

pre-commit:
  parallel: true # Exécute les commandes en parallèle pour plus de rapidité
  commands:
    # Formatage automatique avec Prettier (global)
    prettier:
      glob: '**/*.{js,jsx,ts,tsx,css,md,json,yml,yaml}'
      run: npx prettier --write {staged_files}
      stage_fixed: true # Re-ajoute automatiquement les fichiers corrigés
      fail_text: '❌ Prettier formatting failed. Please check the output above.'

    # Linting ESLint pour l'API (backend)
    eslint-api:
      glob: 'api/src/**/*.js'
      run: cd api && npm run lint:fix
      stage_fixed: true
      fail_text: '❌ ESLint API failed. Please fix the linting errors above.'

    # Linting ESLint pour le client (frontend)
    eslint-client:
      glob: 'client/src/**/*.{js,jsx,ts,tsx}'
      run: cd client && npm run lint:fix
      stage_fixed: true
      fail_text: '❌ ESLint client failed. Please fix the linting errors above.'

    # Vérification TypeScript pour le client
    typecheck-client:
      glob: 'client/**/*.{ts,tsx}'
      run: cd client && npm run type-check
      fail_text: '❌ TypeScript check failed. Please fix the type errors above.'

    # Vérification de la structure du projet
    check-structure:
      glob: '*'
      run: |
        if [ ! -d "api" ] || [ ! -d "client" ]; then
          echo "❌ Structure monorepo manquante (api/ et client/ requis)"
          exit 1
        fi
        echo "✅ Structure monorepo validée"

# Vérification des messages de commit
commit-msg:
  commands:
    validate-commit:
      run: node scripts/validate-commit-msg.js {1}
      fail_text: |
        ❌ Message de commit invalide!

        Format OBLIGATOIRE: <emoji><type>: <description>

        Types autorisés avec emojis:
        - 🚀feat: nouvelle fonctionnalité
        - 🐛fix: correction de bug
        - 📄docs: documentation
        - 💅style: formatage
        - ♻️refactor: refactoring
        - 🧪test: ajout de tests
        - 🔧chore: maintenance
        - 👷ci: intégration continue
        - ⚡perf: amélioration de performance
        - 📦build: build/dépendances
        - ⏪revert: annulation de commit

        Exemples VALIDES:
        ✅ 🚀feat: add user authentication
        ✅ 🐛fix: resolve database connection
        ✅ 📄docs: update API documentation

        Exemples INVALIDES:
        ❌ feat: add user authentication (manque emoji)
        ❌ 🚀 feat: add user authentication (espace)
        ❌ 🚀feat: Add user authentication (majuscule)

        Voir COMMIT_CONVENTIONS.md pour plus de détails.

# Vérifications avant push
pre-push:
  commands:
    # Audit de sécurité npm pour l'API
    security-audit-api:
      run: |
        echo "🔒 Audit de sécurité API..."
        cd api && npm audit --audit-level=moderate
      fail_text: "❌ Vulnérabilités de sécurité détectées dans l'API!"

    # Audit de sécurité npm pour le client
    security-audit-client:
      run: |
        echo "🔒 Audit de sécurité Client..."
        cd client && npm audit --audit-level=moderate
      fail_text: '❌ Vulnérabilités de sécurité détectées dans le client!'

    # Tests backend (si disponibles)
    test-api:
      run: |
        if [ -f "api/package.json" ] && grep -q '"test"' api/package.json; then
          echo "🧪 Exécution des tests backend..."
          cd api && npm test
        else
          echo "ℹ️ Pas de tests backend configurés, skip"
        fi
      fail_text: '❌ Tests backend échoués. Corrigez avant de push!'

    # Build du frontend pour vérifier les erreurs
    build-client:
      run: |
        echo "🏗️ Vérification du build frontend..."
        cd client && npm run build
      fail_text: '❌ Build client échoué. Corrigez les erreurs avant de push!'

    # Vérification finale du linting (sans auto-fix)
    final-lint-check:
      run: |
        echo "🔍 Vérification finale du linting..."
        cd api && npm run lint
        cd ../client && npm run lint
      fail_text: '❌ Erreurs de linting détectées. Corrigez avant de push!'

    # Vérification de la santé de l'API (si elle tourne)
    api-health:
      run: |
        if curl -f -s http://localhost:3001/api/health >/dev/null 2>&1; then
          echo "✅ API health check: OK"
        else
          echo "ℹ️ API non accessible (normal si pas démarrée)"
        fi

    # Vérification de la base de données (si Docker tourne)
    db-health:
      run: |
        if docker ps | grep -q postgres; then
          echo "🔍 Vérification PostgreSQL..."
          if docker-compose exec -T postgres pg_isready -U postgres -d gymart >/dev/null 2>&1; then
            echo "✅ PostgreSQL: OK"
          else
            echo "⚠️ PostgreSQL non accessible"
          fi
        else
          echo "ℹ️ PostgreSQL non démarré (normal en développement)"
        fi

# Célébration après commit réussi
post-commit:
  commands:
    celebrate:
      run: echo "✅ Commit GymArt validé! 🏋️ Code formaté et vérifié automatiquement."
